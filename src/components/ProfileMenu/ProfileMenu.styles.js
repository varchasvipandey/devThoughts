import styled, { css } from "styled-components";

export default styled.div(
  () => css`
    .info {
      text-align: center;
      font-size: 1.6rem;
      font-weight: 600;
      color: var(--color-logo-1);
    }
  `
);
